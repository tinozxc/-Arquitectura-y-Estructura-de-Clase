package Chapter3_Java.Day6_ArrayList_Fechas;

//varias formas de declarar un array 2D (todas validas, pero confusas )
public class CreatingAMultidimensionalArray {
    /*
    *   int[][] vars1;      // 2D array
        int vars2 [][];      // 2D array
        int[] vars3[];       // 2D array (mezcla estilos, pero sigue siendo válido)
        int[] vars4[], space[][];   // vars4 es 2D, space es 3D  la misma línea
    *
    * */

    //Crear un array 2D con tamaño definido
    //String[][] rectangle = new String[3][2];

    /*
    * Crear un array asimétrico definiendo cada fila por separado
         int[][] args = new int[4][];   // solo defines el tamaño de la PRIMERA dimensión
         args[0] = new int[5];          // defines el tamaño de esa fila específica
         args[1] = new int[3];          // otra fila, con tamaño distinto
//       args[2] y args[3] quedan como null hasta que los definas
    * */

}
